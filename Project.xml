<?xml version="1.0" encoding="utf-8"?>
<project>
	<!-- App -->

	<app title="Friday Night Funkin': ALE Psych" file="ALEPsych" packageName="com.alepsychcrew.alepsych" package="com.alepsychcrew.alepsych" main="core.Main" version="0.0.1" company="ALEPsychCrew"/>

	<window width="1280" height="720" fps="120" background="#000000" hardware="true" vsync="false"/>
	<window if="desktop" orientation="landscape" fullscreen="false" resizable="true"/>
	<window if="mobile" orientation="landscape" fullscreen="true" width="0" height="0"/>

	<icon path="art/icon16.png" size='16'/>
	<icon path="art/icon32.png" size='32'/>
	<icon path="art/icon64.png" size='64'/>
	<icon path="art/iconOG.png"/>

	<!-- ALE Defines -->

	<define name="ALLOW_HSCRIPT"/>
	
	<define name="ALLOW_LUA" if="cpp"/>

	<define name="ALLOW_CRASH_HANDLER"/>

	<define name="ALLOW_DISCORD" if="windows || linux || macos"/>

	<define name="ALLOW_VIDEOS" if="cpp" unless="switch"/>

	<define name="ALLOW_LINUX_API" if="linux cpp" unless="switch"/>
	<define name="ALLOW_WINDOWS_API" if="windows cpp"/>
	
	<define name="ALLOW_MOBILE_API" if="mobile cpp"/>

	<define name="ALLOW_ALL_CLASSES"/>

	<define name="FORCE_TOUCH" if="mobile || switch"/>
	
	<!-- Assets -->

	<source path="source"/>
	
	<assets path="assets" embed="false"/>
	<assets path="defines" embed="false"/>
	<assets path="content" embed="false"/>
	<assets path="mods" embed="false"/>

	<!-- Libraries -->

	<haxelib name="flixel"/>
	<haxelib name="flixel-addons"/>
	<haxelib name="flixel-animate"/>
	<haxelib name="flxsoundfilters"/>
	
	<haxelib name="foxlite"/>
	
	<haxelib name="hxdiscord_rpc" if="ALLOW_DISCORD"/>

	<haxelib name="hxgamemode" if="ALLOW_LINUX_API"/>

	<haxelib name="hxvlc" if="ALLOW_VIDEOS"/>

	<haxelib name="sl-windows-api"/>

	<haxelib name="rulescript" if="ALLOW_HSCRIPT" unless="ALE_HSCRIPT"/>

	<haxelib name="ale-rulescript-wrapper" if="ALLOW_HSCRIPT" unless="ALE_HSCRIPT"/>
	<haxelib name="ale-foxlite-wrapper"/>

	<haxelib name="ale-hscript" if="ALLOW_HSCRIPT ALE_HSCRIPT"/>

	<haxelib name="hxluajit" if="ALLOW_LUA"/>
	<haxelib name="hxluajit-wrapper" if="ALLOW_LUA"/>

	<haxelib name="jsonmod"/>

	<haxelib name="ale-ui"/>

	<haxelib name="extension-androidtools" if="android"/>
	<haxelib name="extension-orientation" if="mobile"/>
	
	<!-- Flags -->

	<set name="BUILD_DIR" value="export/debug" if="debug"/>
	<set name="BUILD_DIR" value="export/release" unless="debug"/>
	<set name="BUILD_DIR" value="export/32bit" if="32bits"/>

	<haxeflag name="-dce" value="no"/>

	<!-- Defines -->
	
	<set name="SWF_VERSION" value="11.8"/>
	
	<define name="HX_WINDOWS" if="ALLOW_WINDOWS_API"/>

	<haxedef name="DISCORD_DISABLE_IO_THREAD" if="hxdiscord_rpc" />

	<haxedef name="hscriptPos"/>

	<haxedef name='lime_disable_assets_version'/>

	<haxedef name="FLX_NO_TOUCH" if="desktop"/>
	<haxedef name="FLX_NO_FOCUS_LOST_SCREEN"/>
	<haxedef name="FLX_NO_SOUND_TRAY"/>
	<haxedef name="FLX_NO_HEALTH"/>
	<haxedef name="FLX_NO_DEBUG"/>

	<haxedef name="HXCPP_CHECK_POINTER" if="ALLOW_CRASH_HANDLER"/>
	<haxedef name="HXCPP_STACK_LINE" if="ALLOW_CRASH_HANDLER"/>
	<haxedef name="HXCPP_STACK_TRACE" if="ALLOW_CRASH_HANDLER"/>
	<haxedef name="HXCPP_GC_BIG_BLOCKS"/>
	<haxedef name="HXCPP_DEBUG_LINK"/>
	<haxedef name="HXCPP_CATCH_SEGV"/>

	<haxedef name='no-deprecation-warnings'/>
	<haxedef name="message.reporting" value="pretty"/>
	
	<!-- _________________________________ Macros ________________________________ -->
	
	<haxeflag name="--macro" value="core.macros.Macros.init()"/>

	<!-- Android -->

	<config:android>
        <gradle-properties org.gradle.jvmargs="-Xmx2048m -XX:MaxMetaspaceSize=512m"/>
    </config:android>
	
	<config:android permission="android.permission.READ_EXTERNAL_STORAGE" if="android"/>
	<config:android permission="android.permission.WRITE_EXTERNAL_STORAGE" if="android"/>

	<config:android permission="android.permission.MANAGE_EXTERNAL_STORAGE"/>
	<config:android permission="android.permission.REQUEST_INSTALL_PACKAGES"/>

	<config:android permission="android.permission.INTERNET"/>
	<config:android permission="android.permission.ACCESS_NETWORK_STATE"/>

	<certificate path="key.keystore" password="aleenginepass" alias="ale-engine" alias-password="aleenginepass" if="android"/>

	<!-- Switch -->

	<section if="switch">
		<haxedef name="lime-opengl"/>
		<haxedef name="lime-cairo" value="false"/>

		<set name="LIME_CAIRO" value="0"/>
		<set name="LIME_OPENGL" value="1"/>

		<config:switch ip="192.168.1.140"/>

		<haxelib name="hx_libnx" />
		<haxelib name="flixel-switch" />

		<config:switch libs="lua5.1" if="ALLOW_LUA" />
	</section>
</project>
